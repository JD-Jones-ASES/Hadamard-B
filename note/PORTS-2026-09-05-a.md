# Port note — 2026-09-05, lane a

**Ported** from the source laboratory's NOTE-B draft 2 (`Hadamard-2060`,
`intel/fleet-2026-09-01/skeptic-pass/NOTE-B-draft-2.md`; decisions **D-062**,
**D-068**): its four audited theorems and three certificates, queued since
D-068 and adopted there after replay and an independent audit. Replays are
this worktree's; all exit 0.

| ported | to | cert | default | `--full` |
| --- | --- | --- | --- | --- |
| Theorem T (closure under transposition) + remark R | §1.9 | **26** (written here) | 46 checks, 15.5–16.9 s | **run here** — 64 checks, 45.4 s |
| Sylow-2 rigidity of a cocyclic `H(4t)` | §1.10 | **27** (lab `0026`) | 14 checks, 12.5 s | **run here** — 13.9 s |
| the `(2,4)` existence theorem | §2.5 | **28** (lab `0027`) | 14 checks, 0.7 s | none — census exhaustive by default |
| Lemma F and the 668 / 716 records | §1.11 | **29** (lab `0028`) | 40 checks, 1.3 s | priced ≈ 10.7 core-hours, **not run here** |

`python verify/verify.py --selftest` → `SELFTEST: PASS`. Also ported: forward
pointers from §1.4, §2.3, §2.4 and the §3 preamble; the Lemma-F sentence in
§3.4 and §3.6; nine rows in §5's label table; four rows in README's replay
block and its two movement paragraphs; a dated section in PROVENANCE. **§2.5,
not a second half of §2.4:** §2.4 already carries two things, and a third
theorem with a six-step proof would overload it; §2.4(c) forward-points to
§2.5, which says outright that cert 18's census and cert 28 are two independent
proofs of one statement.

**Changed in transit.**
* **Theorem T stated branch-free** — the draft's route through `EEᵀ = 4s·I`
  holds only in the house branch under `w > 2s`; §1.9 uses Theorem A's *only
  if* half instead (the desk's repair, D-068.8a), so it holds at every `(s,i)`
  and every `w`. **Remark R restored** (same repair) and strengthened: cert 26
  evaluates its condition and `verify/verify.py`'s verdict on all seven banked
  records and asserts they agree — true at 668, 716, 1676, 1772; false at 1916,
  1388, 1436.
* **§1.11 uses `N` for the order** throughout, per §1.0 (D-068.8e), and its
  records claim is **PROVEN-BY-CERTIFICATE**, not the draft's
  COMPUTATIONAL-EVIDENCE: the lab's `--full` ran on all four records
  2026-09-03/04 (9 044.0 / 9 062.2 / 9 049.1 / 11 273.6 s), the second
  implementation. §1.11 and cert 29's NOTES say **where** it ran; it has not
  been run here, and cert 29's default path audits.
* **Cert 29's three lab-only inputs rewired** onto `data/payload-records.json`
  and `data/twisted-i2-records.json`, both file-pinned, with the `ψ`-twist
  re-derived in the run. The third — a *text* audit of the finder's own logs —
  was **dropped**; in its place, the twisted record's assembled digest against
  the `pinned_sha256` the data file carries, plus an internal consistency check
  on the pinned partition. **Cert 28's scratch** is now a private temporary
  directory, and it cites cert 18 where the lab cited its own `0022`.
* **Cert 26 is authored here and named `26-theorem-t-structure`**, not the
  briefed `26-theorem-t-smallorders`: the entrywise identities are its
  headline, the census is clause [6]. The lab's `transpose_smallorders.py` is
  runnable but not portable — it reads `experiments/pr0031`, the lattice
  `gate44_instances.json`, lab `cell24/` copies and a shared library — so
  cert 26 builds its **own** census from instances this repository can
  construct: 11 of them, 7 separated, 4 undecided under `--full` (the default
  path reports 9 instances, 5 separated, 4 undecided), with the lab's 44-instance
  census cited as a source-laboratory measurement.

**Not ported.** The draft's corollary table (already §3's, certs 08/15/19/20/21);
the lab's finder-side isomorphism search and its 44-instance totals (cited,
never replayed); "Theorem AP", a sketch; C9b.2–C9b.4 and C9b.6 (Ito,
GS-cocyclic, BH/qLP, LP compression), which survived pass II but are not
theorems of this note; the two order-20 boundary records, which cert 26's
census cannot build. **DISCLOSURE.md untouched** — no stance changed.
