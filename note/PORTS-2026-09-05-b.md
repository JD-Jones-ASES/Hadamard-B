# Port note — 2026-09-05, lane b (the general branch, and the product theorem)

Three results proved and certified in the source laboratory on 2026-09-04,
each adopted there with a written proof and a desk replay, ported here in
house form. Nothing here imports or opens anything outside this repository,
and nothing here is a statement about `H(2092)`.

| where | what |
| --- | --- |
| `note/NOTE-B.md` §1.12 | Theorem F in the **general branch** (`M = 4i·P_S`, no use of `i = s+1`); the **row-factorisation lemma** `WWᵀ = 4i·4s·(I₄⊗P_S)`; the **dual form**; the `(7,12)/S3` **border kill**, DEAD-BY-CERTIFICATE at every order `4(12w+7)`; and the scope paragraph |
| `note/NOTE-B.md` §1.13 | **Theorem G**, the good-matrix product theorem with the general prefactor `−(b₀c₀d₀)`, the group-ring proof, and the closure sign law |
| `note/NOTE-B.md` §2.6 | what certs 30 and 31 prove, and what they do not |
| `note/NOTE-B.md` §5 | five label rows, appended at the end of the table |
| `note/NOTE-B.md` §2.4 | one forward pointer in the "general branch at 2092" remark; no other existing text touched |
| `certs/30…32/` | 59 / 50 / 73 checks; **0.10 s**, **1.07 s**, **1.18 s**; exit 0 each; no `--full` anywhere — everything is run |
| `data/` | `general-branch-sigma-tables.json`, `q-7_12-S2.json` |
| `README.md`, `PROVENANCE.md` | cert rows and a dated provenance paragraph, both appended at the end |

## Three judgement calls, recorded

**1. The dual form was earned, so it is in.** The source laboratory's desk
note carried it as a two-line remark labelled *"PROVEN modulo a lane
re-derivation"*. It was re-derived here from the row-factorisation lemma
before being written down — the proof in §1.12 is this repository's — and it
is checked in cert 31 [F] on cert 18's four `(2,4)` records, exhaustively over
all 256 admissible rows, and on `(3,4)` kits built in the run. Provenance:
*desk note, re-derived here*.

**2. Cert 32 is an addition to the brief; the desk should rule on it.** The
port was asked for certs 30 and 31, and for §1.13's controls as
COMPUTATIONAL-EVIDENCE. But those controls are computational claims, and this
repository's standing rule is that *every computational claim in the note
carries a certificate* (README, DISCLOSURE). Rather than put uncertificated
counts into the note, the port wrote `certs/32-good-product-theorem/`.
Number **32** was chosen because lane a holds 26–29; renumber freely.

**3. One count in the source decision reads differently here.** That
decision's Williamson control cites "48 of 960 at `n = 7`" for the doubling
relation. Re-enumerated here, the theorem's own prefixed form holds in **0**
of all 14 784 Williamson quadruples, at every order including `n = 7`; the 48
belong to the *unprefixed* variant `a_{2k} = −a_kb_kc_kd_k`. Cert 32 checks
and reports both, and `PROVENANCE.md` records the difference. The control's
purpose — showing that skewness of `A` is load-bearing — is only stronger
under the corrected reading.

## Merge

Lane a inserts §§1.9–1.11 at the same point in Movement I and may add a §2.5:
expect a textual conflict at the end of Movement I and possibly at the end of
Movement II. Both sides are pure insertions — take both and renumber. Nothing
else here touches existing text, and no certificate 01–25 was modified.

## Open, and not claimed anywhere here

No border **kit** is known in the general branch at 2092; the `Q` quantifier
is open at `S1`, `S2` and `S4`, and settled only at `S3`, where it is empty.
No **seed** quadruple is known at any general-branch cell at 2092, nor any
good quadruple at `n = 523`. The source laboratory has an unaudited lane
generalising the forced-multiset derivation; nothing from it is banked here.
